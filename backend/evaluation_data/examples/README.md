# Example Error Analysis Outputs

Run the evaluation command to generate JSON reports under `evaluation_data/output/`.

The checked-in examples in this folder are snapshots of the expected report shape:

- per-mode JSON reports with task summaries
- non-exact cases listed as inspectable errors
- combined multi-mode summary in `combined_report.json`
